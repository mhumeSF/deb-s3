# Default checks, minus ST1005: command errors are printed verbatim as
# user-facing CLI output, so some deliberately read as full sentences (see
# executeCommand in internal/cli/root.go).
checks = ["all", "-ST1000", "-ST1003", "-ST1016", "-ST1020", "-ST1021", "-ST1022", "-ST1023", "-ST1005"]
